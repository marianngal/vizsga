<Window x:Class="Restaurant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant"
        mc:Ignorable="d"
        
        Title="Restaurant"
        SizeToContent="WidthAndHeight"
        FontSize="24">    
    
    <Window.DataContext>
        <local:Menu/>
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <DataTemplate x:Key="ListaElem" DataType="local:Pizza">
            <WrapPanel>
                <Label Content="{Binding Size}" />
                <Label Content="{Binding Price}" />
                <Label Content="{Binding Name}" />
            </WrapPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="250" Width="5*" />
            <ColumnDefinition MinWidth="250" Width="4*" />
        </Grid.ColumnDefinitions>

        <ListBox Margin="10" Grid.Column="0"
            ItemsSource="{Binding Items}" 
            SelectedItem="{Binding Selected, Mode=TwoWay}"
            ItemTemplate="{StaticResource ListaElem}"/>
       
        <StackPanel Margin="10" Grid.Column="1">
            <Label>Név</Label>
            <TextBox
                Text="{Binding Selected.Name, Mode=TwoWay}"></TextBox>
            <Label>Ár</Label>
            <TextBox
                Text="{Binding Selected.Price, Mode=TwoWay}"></TextBox>
            <Label>Méret</Label>
            <TextBox
                Text="{Binding Selected.Size, Mode=TwoWay}"></TextBox>
        </StackPanel>
            
    </Grid>
</Window>
